# 旅行短视频剪辑导演助手 PRD v1.2

## 1. 产品目标

一期目标是把旅行短视频剪辑前期最容易混乱的部分结构化，帮助用户稳定完成以下工作：

- 管理项目基础信息
- 录入和维护素材清单
- 基于素材生成主题方向
- 先做节奏规划，再生成分镜时间线
- 在导出阶段整理标题、标签和文案

一期不做：

- 自动成片
- 自动识别真实音频节拍文件
- LLM 自动生成标题、标签和文案
- 自动策略性复用同一素材（三期 Sprint 15 起可通过项目开关 `allowAssetReuse` 启用，默认仍关闭）

说明：

- 如果 PRD 与当前代码逻辑冲突，以当前已实现逻辑为准。
- 二期再在现有接口上补充视频分析、真实节拍识别和 LLM 建议生成。

## 2. 目标用户

- 旅行内容创作者
- 剪辑师
- 运营同学

## 3. 核心价值

- 把“素材很多、不会组织”的问题，转成可执行 workflow。
- 保证每个分镜都绑定真实素材，降低臆造风险。
- 让节奏规划先行，避免分镜写完后才发现卡点对不上。

## 4. Workflow

固定顺序：

1. 新建
2. 录入
3. 主题
4. 节奏
5. 分镜
6. 导出

阶段依赖：

- 没有项目，不能进入素材录入
- 没有素材，不能进入主题
- 没有选定主题，不能进入节奏
- 没有节拍点，仍可生成分镜，但若关闭节拍适配则按素材建议时长生成
- 没有分镜，不能进入导出

## 5. 功能需求

## 5.1 项目管理

首页展示历史项目列表，支持：

- 新建项目
- 进入项目
- 进入素材列表
- 删除项目

项目字段：

- 项目名称
- 目的地
- 发布平台
- 目标时长
- 视频类型
- 风格偏好
- 风格补充说明
- 路线
- 素材根目录
- 状态

规则：

- 新建项目默认状态为 `draft`
- 项目级保存素材根目录
- 当前不允许在新建项目时手动选择状态

## 5.2 素材 CRUD

素材列表页支持：

- 查看素材列表
- 新增素材
- 编辑素材
- 删除素材

素材字段：

- 系统自动生成素材 ID
- 地点
- 画面内容
- 素材相对路径
- 视频类型
- 镜头类型
- 信息量
- 建议时长
- 情绪标签
- 视觉标签
- 功能标签

镜头类型枚举：

- 大景
- 中景
- 中景含人物/主体
- 特写
- 人物
- 动物
- 转场

说明：

- “中景含人物/主体”表示同时包含景别信息和人物/主体语义。
- 功能标签支持推荐选项 + 自定义输入。
- 如果一个原始视频中包含多个可独立使用的镜头，建议用户先人工切分，再分别作为多个素材录入系统。

## 5.3 主题生成与选择

功能：

- 基于项目和素材生成 3 个主题方向
- 用户选择一个当前主题

输出字段：

- 标题
- 摘要
- 核心情绪
- 预期节奏
- 平台理由
- 是否当前选中

规则：

- 当前主题保存在项目上
- 后续节奏和分镜都依赖当前主题

## 5.4 节奏规划

功能：

- 生成节奏方案
- 人工编辑后保存

字段：

- BGM 风格
- 参考曲目
- 节拍模式
- 节拍点
- 节奏说明
- 暗场建议点
- 照片动效建议

规则：

- 分镜生成可选择是否适配节拍。
- 支持规则生成 beat points，也支持上传音频文件做基础节拍识别。
- 音频上传支持 WAV、MP3、M4A、AAC、OGG、MGG、 FLAC、WMA；非 WAV 格式需本机安装 ffmpeg 转码。
- 识别结果写入 `analysisSource`（`rule` / `audio_upload` / `manual`），失败时可回退规则生成。
- 音频上传后优先使用 librosa 识别真实节拍；不可用时回退能量起音检测。
- 支持剪映对标踩点模式：踩节拍1（粗密度）、踩节拍2（细密度）、强弱拍；切换模式基于 `rawBeatPoints` 重采点。
- 规则生成路径仍保留，作为无音频或识别失败时的兜底。

## 5.5 分镜生成

功能：

- 基于当前主题、素材、目标时长、节拍点生成时间线
- 生成后允许人工微调并保存

字段：

- startTime
- endTime
- assetId
- shotDescription
- function
- rhythm
- beatMode
- beatPoints
- subtitle

一期生成规则：

- 所有分镜必须绑定真实素材 ID
- 一期默认不自动重复使用同一素材（项目开启 `allowAssetReuse` 后，生成逻辑可轮用素材以贴近目标时长）
- 分镜生成顺序必须在节奏规划之后
- 分镜生成时，素材采用“路线主轴 + 叙事槽位”的两层编排：
- 第一层按项目路线约束素材顺序，优先保证旅行和攻略内容的地点连续性
- 第二层按平台节奏画像生成叙事槽位，例如 `hook`、`turn_1`、`turn_2`、`climax`、`payoff`、`chapter`、`proof`、`summary`
- 反转和高潮是关键点，不是整段标签；系统需要在关键点之间自动插入 `setup`、`develop_1`、`develop_2`、`detail`、`experience` 等中间状态
- 每个槽位根据素材的 `shotType`、`informationDensity`、`functionTags`、`emotionTags`、`visualTags` 进行评分选材
- 开头钩子允许使用后段强视觉素材做预告，但正文需要回到路线起点
- `rhythm_hit` 和 `transition_buffer` 不再作为全局章节标签使用，而是作为槽位内选材评分信号参与微调
- 若开启“适配节拍”，镜头时长优先贴合节拍点
- 若关闭“适配节拍”，镜头按素材建议时长顺排
- 当素材全部使用完仍不足目标时长时，系统停止生成，并以当前可生成的最大时长为准
- 系统需要明确提示“素材不足”，并建议用户补充素材，或先将长素材切分后分别录入

说明：

- 如果确实需要同一原始视频里的多个镜头，请优先通过“人工先切分，再作为多个素材录入”的方式解决；若项目已开启镜头复用，则生成阶段可多次引用同一素材 ID，validation 会提示复用段数与时长占比（非硬阻断）。

校验结果需要返回：

- 是否全部绑定素材
- 是否启用镜头复用（`assetReuseEnabled`）
- 复用素材数 / 额外复用段数（`reusedAssetCount` / `reusedSegmentCount`）
- 是否通过地点连续性检查
- 是否启用节拍适配
- 若启用节拍适配，是否通过节拍对齐检查
- 当前总时长
- 是否达到目标时长
- 结果说明文案

列表与编辑交互：

- 分镜列表页只显示摘要信息，不做整表单编辑
- 单条分镜进入独立编辑页处理详细字段
- 编辑页保存后自动返回分镜列表
- 列表页保留的动作包括：
  - 编辑
  - 删除
  - 在当前镜头后插一条
  - 上移 / 下移

## 5.6 导出

导出阶段当前做两件事：

1. 人工填写导出信息
2. 预览导出脚本

导出信息字段：

- 标题
- 短标题
- 标签
- 文案
- 封面建议
- 整段口播稿 `voiceoverScript`：可手动输入，也可先基于标题、标签和文案生成草稿；当前只作为导出内容和后续 TTS 输入，不自动生成音频
- 口播生成参数：`voiceoverProvider`、`voiceoverVoice`、`voiceoverStyle`、`voiceoverSpeed`、`voiceoverEmotion`。Edge 支持智能匹配及常用中文男女声选择；具体音色优先于风格和情绪自动匹配，剪映原生朗读音色仍在剪映内选择
- 口播密度 `voiceoverDensity`：支持 `light`（轻口播）、`standard`（标准口播）、`info`（信息口播）。剪映原生朗读轨会基于该配置把逐镜头字幕压缩为跨多个镜头的口播块，避免一倍速朗读跟不上快切画面
- 口播生成状态：`voiceoverGenerationStatus`、`voiceoverDurationSec`、`voiceoverAudioPath`、`voiceoverProviderMeta`、`voiceoverGeneratedAt`；当前用于检查口播文本与 Provider 是否就绪，并估算旁白时长
- `mock_silence` Provider：生成本地占位静音 WAV，用于验证口播音频链路、下载、试听和剪映草稿口播音轨接入；不代表真实 AI 口播。占位 WAV 按分镜时间线总时长生成，避免导入剪映后口播占位轨比视频/BGM 更短
- `jianying_native_tts` Provider：导出剪映草稿时生成“最终字幕（剪映朗读源）”文本轨，用户打开剪映专业版后可选择该文本轨并使用剪映内置“开始朗读”生成口播；系统不直接模拟点击剪映 UI，也不额外产生云端 TTS 成本
- Provider 能力表由后端统一提供：当前 `mock_silence`、`jianying_native_tts` 与 `edge` 可用；`edge` 通过微软在线神经网络音色生成真实 MP3，`openai`、`custom` 继续作为后续接入预留。未启用 Provider 不会触发真实音频生成
- `edge` Provider 根据用户指定音色，或按口播风格、情绪和语速智能匹配中文音色并生成真实口播，保存实际音频路径、时长、音色 ID、中文名称、速率与格式元数据；重新生成使用唯一音频文件并禁用试听缓存，确保切换音色后立即生效
- `edge` Provider 与剪映原生朗读共用压缩后的最终字幕块，不再分别消费整段口播稿和原逐镜头字幕。合成时请求 `WordBoundary` 元数据并保存每个字幕块的真实发音起止时间；剪映草稿使用这些时间生成“最终字幕（口播同步）”轨，保证显示文本与朗读内容一致
- 剪映草稿导出：若存在口播音频，导出时新增独立口播音轨；若同时存在 BGM，BGM 默认降低音量，避免盖住人声
- 剪映草稿导出：若选择 `jianying_native_tts`，导出时只生成一条“最终字幕（剪映朗读源）”文本轨；优先使用逐镜头口播文案，没有逐镜头口播时使用字幕，若逐镜头均为空则使用整段口播稿
- 最终字幕按口播密度合并和压缩，时间范围与朗读块完全一致；同一份文本同时用于屏幕字幕和剪映朗读，避免口播与字幕不一致
- 选择“剪映原生朗读”后，页面实时展示压缩前后字数、压缩比例、口播块数量、预计朗读时长，并可展开检查每个口播块的时间范围和文本，便于在导出前判断一倍速是否可读完
- 口播压缩优先保留完整短句并去重，避免按字符硬截断产生半句话；后续接入 LLM 时可在同一预览链路中增加语义改写
- 剪映朗读轨按字数、语速和标点停顿估算真实朗读时长，并预留结尾发音余量；末尾分镜时间不足时优先向前借用无口播区间，不得使用省略号硬截断或直接切断尾音
- 口播预览需要分别显示“朗读是否完整”和“是否与原画面对齐”。轻微偏差可应用系统建议倍速；超过 `1.30x` 仍无法对齐时，应提示精简文案或延长对应画面，不应继续机械提速
- 剪映原生朗读暂不能由草稿文件直接控制生成音色的真实语速，系统会将校准倍速写入口播轨名称，用户在剪映执行朗读后按该值人工调整；后续真实 TTS Provider 可直接消费同一参数
- 剪映草稿根据分镜策略自动落地基础粗剪效果：缓冲位使用内置叠化，照片慢推/轻缩放使用等比缩放关键帧，钩子、反转、高潮和收尾字幕适度强化，BGM 默认使用 0.4 秒淡入和 1 秒淡出
- 剪映字幕默认字号为 6.0，重点字幕最高 6.6；长字幕按字符数分级缩小且最低为 4.2，字幕位置上移至底部安全区，避免同步口播长句或重点字幕溢出画面
- 硬切与干净切不写入额外转场材料；视频素材不自动套用照片缩放动效，避免为了“有效果”而破坏原始运动

预览格式：

- Markdown
- JSON
- YAML
- 口播稿 TXT

规则：

- 导出内容必须带上标题、标签、文案和分镜时间线
- 当前为人工填写
- 二期再补 LLM 建议生成，但字段结构保持不变

## 6. 页面要求

## 6.1 首页

- 历史项目列表
- 右上角新建项目按钮
- 删除项目二次确认

## 6.2 项目页

- 上方展示项目信息
- 中间展示可点击的 workflow stepper
- 下方展示项目编辑和数据状态

说明：

- workflow stepper 本身承担导航作用
- 不再额外做一套重复的 workflow 导航卡片

## 6.3 各阶段页面

- 保留 breadcrumb
- 保留 workflow stepper
- 当前 step 高亮必须与实际页面一致
- stepper 中已解锁阶段可点击跳转

## 6.4 分镜列表页

- 顶部保留“生成分镜”入口
- 提供“是否适配节拍”开关
- 明确提示当前一期默认不重复素材
- 列表每条分镜仅显示：
  - 时间范围
  - 素材 ID
  - 镜头描述
  - 功能标签
- 列表页不出现“保存整表分镜”按钮
- 所有详细编辑动作下沉到单镜头编辑页

## 6.5 单镜头编辑页

- 用于快速修正单条分镜
- 保存后自动返回分镜列表
- 允许编辑：
  - 开始时间
  - 结束时间
  - 素材 ID
  - 功能标签
  - 镜头描述
  - 节奏说明
  - 节拍模式
  - 字幕建议
  - 当前镜头节拍点

## 7. 当前已实现范围

- 项目 CRUD
- 素材 CRUD
- 主题生成与选择
- 节奏规划生成与保存
- 分镜生成与保存
- 导出信息保存
- Markdown / JSON / YAML 预览导出

## 8. 二期扩展方向

- 真实歌曲节拍识别
- 音频文件上传与节拍自动识别
- 节拍识别失败时回退到规则生成
- 视频内容自动分析
- 素材自动标签补全
- 基于大模型的分镜排序建议：结合主题、路线、地点连续性、镜头类型和情绪节奏，对素材顺序做智能编排
- 保留一期规则排序作为兜底方案；当大模型排序不可用或结果不可信时，回退到两层规则排序
- LLM 自动生成标题、标签、文案
- 自动粗剪

## 8.1 节奏字段升级边界

一期继续保留规则生成的字段：

- `beatMode`
- `beatPoints`
- `darkCutSuggestions`
- `photoMotionSuggestions`
- `selectedTrackName`
- `rhythmNotes`

当前实现说明（Sprint 4 后）：

- `beatPoints`：音频上传时由 librosa / 能量检测识别；规则生成时由目标时长 + 剪映踩点语义估算；识别失败回退规则生成。
- `beatMode`：音频上传时按 BPM 与节拍间隔推荐；语义对齐剪映（踩节拍1 粗 / 踩节拍2 细 / 强弱拍）。
- `rawBeatPoints` / `coarseBeatPoints`：分别缓存细粒度起音与粗粒度强拍序列；切换 `beatMode` 时不需重新上传音频。
- `audioFingerprint` / `audioAnalysisVersion`：音频上传后保存内容指纹和识别引擎版本；同一音频重复上传时可复用上次保存的剪映参考点和整体偏移。
- `selectedTrackName`：优先使用上传音频文件名；否则保留用户填写值；规则生成时使用「主题名-参考曲」。
- `darkCutSuggestions`：音频上传时基于能量局部低谷推断；无音频时回退 25%/50%/75% 结构点。
- `bgmStyle` 和 `rhythmNotes`：配置 LLM 时优先生成；失败回退模板/规则文案。

四期 P4-A 已开始新增：

- `rhythmProfile`：按平台、视频类型和目标时长生成平台节奏画像，区分抖音/快手、小红书、B 站攻略、B 站 Vlog、视频号和 YouTube 等节奏生态。
- `attentionBeats`：内容注意力点，承接“时钟法则”和“射门集锦法则”，用于标记钩子、推进、反转、高潮和收尾。不同平台使用不同角色模板：抖音/快手强调钩子、两次反转、高潮和记忆点回收；小红书强调视觉种草、信息价值、决策推动和收藏提示；B 站攻略强调章节节点、信息证明和总结回收；B 站 Vlog 强调沉浸、情绪转折和留白回味。
- `beatCalibration`：节拍校准参数，当前支持整体偏移 `beatOffsetSec`、轻微比例校准 `beatScale`、剪映参考节拍点 `referenceBeatPoints` 和密度模式 `densityMode`；保存后分镜生成与前端节拍吸附会使用校准后的节拍点。若用户填写少量剪映参考点，系统会估算整体 offset、小范围 scale，并在 `beat_1 / beat_2 / strong_weak` 中推荐更接近剪映的密度模式。
- 节奏页增加节拍校准追踪面板，展示原始识别点、当前模式筛选点、剪映参考点和校准后预览点，方便定位与剪映踩点不一致的问题。
- 分镜段新增 `attentionRole`、`visualStrength`、`motionPolicy`、`transitionPolicy`、`subtitlePolicy`。字幕策略支持按叙事角色自动推断，也允许单镜头覆盖为常规、重点、信息或弱化字幕；后续可接入视觉模型和 LLM 编排。
- 分镜段预留 `voiceoverText`、`voiceoverRole`、`voiceoverTiming`，当前用于手工录入口播文案和导出追溯；P4-B 自动口播会基于这些字段生成旁白音轨。
- 分镜规则生成已开始接入路线主轴和叙事槽位：短视频允许开头强素材预告，正文回到路线顺序；B 站攻略类优先章节化和地点连续性。后续继续补 `narrativeSlotId`、`routeIndex`、`selectionReason` 和 Beam Search 多候选编排。
- 一键初剪提供两种模式：默认“补齐缺失阶段”不覆盖已有人工结果；“从头生成创意方案”会先保存当前结构化方案快照，再使用当前启用的 LLM 重做主题、分镜和导出文案。为避免重复验证音乐，真实音频、识别节拍和人工校准不参与重生成。
- 每次完整重生成记录 Provider、模型与生成时间，为后续模型 A/B 对比、差异查看和版本恢复提供数据基础；版本快照不复制本地媒体文件。

二期升级优先级（剩余）：

1. ~~`beatPoints`~~（已完成）
2. ~~`selectedTrackName`~~（已完成）
3. ~~`darkCutSuggestions`~~（已完成）
4. ~~`beatMode`~~（已完成）
5. ~~`rhythmNotes`~~（已完成）
6. ~~`bgmStyle`~~（已完成）
7. `photoMotionSuggestions`

二期目标（剩余）：

- ~~将 `selectedTrackName` 完全替换为真实音轨名或上传文件名。~~
- ~~将 `darkCutSuggestions` 从固定比例点升级为基于音乐段落变化点的建议。~~
- ~~将 `rhythmNotes` 和 `bgmStyle` 升级为 LLM 辅助生成的可执行建议。~~
## 8.2 二期 LLM 接入要求

二期中，LLM 接入必须设计为“厂商无关配置 + 多认证模式兼容”，不能把产品能力绑定为单一 OpenAI 命名体系。

支持模式：

1. `api_key`
2. `oauth`
3. `device_code`

业务要求：

- 主题建议、分镜建议、导出建议统一走 LLM 网关。
- 上层业务不直接区分 ChatGPT、Kimi、GLM、DeepSeek 等厂商。
- 当前厂商如果仅提供 OpenAI-compatible API，则按 `api_key` 模式接入。
- 当前厂商如果提供官方 OAuth / OIDC，则支持浏览器跳转授权。
- 当前厂商如果提供 Device Code，则支持设备授权流程。

明确边界：

- 只有网页账号登录但没有官方开放协议的产品，不纳入正式接入范围。
- 不允许通过抓取 cookie、session、网页存储令牌等非官方方式接入。
